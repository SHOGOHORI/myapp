<% if @answers.count == 0 %>
  <h3>まだ回答は有りません</h3>
<% else %>
  <h3>回答</h3>
  <div class="card-body">
   <% @answers.each do |answer| %>
     <div class="card" style="width: 100rem;" >
       <li id="answer-<%= answer.id %>">
         <%= link_to gravatar_for(answer.user, size: 50), answer.user %>
         <span class="user"><%= link_to answer.user.name, answer.user %></span>
           <br>
           <span class="card-text">本文：<%= answer.content %></span>
           <br>
           <% if answer.image.attached?  %>
             <%= image_tag post_image(answer.image) %>
           <% end %>
           <br>
           <span class="timestamp">
            <%= time_ago_in_words(answer.created_at) %>前
           </span>
           <% if current_user == answer.user %>
             <%= link_to "削除", answer, method: :delete,
                                              data: { confirm: "削除しますか？" } %>
           <% end %>
       </li>
     </div>
   <% end %>
  </div>
<% end %>