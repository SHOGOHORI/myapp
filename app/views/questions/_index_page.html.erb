<ul class="question-container">
  <div class="question-category">
    <%= render partial: 'category' %>
  </div>
  <% if @search_questions.count == 0 %>
    <h3 class="non-question">質問がありません。</h3>
  <% else %>
    <div class="question-index">
      <h3>検索結果　<%= @search_questions.total_count %>件</h3>
      <%= paginate @search_questions, remote: true %>
        <ul class="questions">
          <section class="question_index">
            <div class="card" >
              <div class="card-body">
                <%= render partial: 'question', collection: @search_questions %>
              </div>
            </div>
          </section>
        </ul>
      <%= paginate @search_questions, remote: true %>
    </div>
  <% end %>
</ul>