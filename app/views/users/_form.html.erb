    <br>
    <%= form_with(model: @user, local: true) do |f| %>
      <%= render 'shared/error_messages', object: f.object %>

      <p class="image_explanation">ユーザー画像</p>
      <%  if @user.image.attached? %>
        <%= image_tag display_image(@user.image), class: 'user-image' %>
        <br>
        <%= link_to "削除", @user.image.purge %>
      <% end %>
      <span class="image">
        <%= f.file_field :image, accept: "image/jpeg,image/gif,image/png" %>
      </span>

      <%= f.label :name, "お名前" %>
      <%= f.text_field :name, class: 'form-control' %>

      <%= f.label :email, "メールアドレス" %>
      <%= f.email_field :email, class: 'form-control' %>

      <%= f.label :password, "パスワード" %>
      <p class="password_explanation">英大文字＋英小文字＋数字＋記号混じりで10桁以上</p>
      <%= f.password_field :password, class: 'form-control' %>

      <%= f.label :password_confirmation, "パスワード確認" %>
      <%= f.password_field :password_confirmation, class: 'form-control' %>

      <%= f.submit btn_name, class: "btn-square-slant" %>
    <% end %>

    <script type="text/javascript">
    $("#user_image").bind("change", function() {
    var size_in_megabytes = this.files[0].size/1024/1024;
    if (size_in_megabytes > 5) {
      alert("5MB以上のファイルは添付できません。");
      $("#user_image").val("");
    }
    });
    </script>